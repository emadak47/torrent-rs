# Async-WSS

## Run

- run w/o logging:
    ```
    cargo r 
    ```
- run w/ logging: (LOG-LEVELS: `TRACE`, `DEBUG`, `WARN`, `ERROR`, `INFO`)
    ```
    RUST_LOG=async-wss=<LOG-LEVEL> cargo r 
    ```
